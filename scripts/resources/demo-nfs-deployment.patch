spec:
  template:
    spec:
      volumes:
      - name: %DEMO_IMAGE_REGISTRY%
        persistentVolumeClaim:
          claimName: %DEMO_IMAGE_REGISTRY%-pvc
      containers:
      - name: %DEMO_IMAGE_REGISTRY%-%REGISTRY_NAME%
        volumeMounts:
        - name: %DEMO_IMAGE_REGISTRY%
          mountPath: /var/lib/registry
          subPath: %REGISTRY_NAME%