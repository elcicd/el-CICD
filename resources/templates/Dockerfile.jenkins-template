FROM %OCP_IMAGE_REPO%/jenkins:latest

COPY ["%JENKINS_CONFIGURATION_FILE%", "%JENKINS_PLUGINS_FILE%", "%CONFIG_PATH%/"]

USER root

RUN /usr/local/bin/install-plugins.sh %CONFIG_PATH%/%JENKINS_PLUGINS_FILE%

USER 1001
