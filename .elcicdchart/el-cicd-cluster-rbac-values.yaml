elCicdTemplates:
- templateName: clusterRole
  appName: elcicd-user
  rules: []

elCicdTemplates-sealed-secrets:
- templateName: clusterRole
  appName: sealed-secrets-management
  mustHaveAnyProfile:
  - sealed-secrets
  labels:
    rbac.authorization.k8s.io/aggregate-to-edit: "true"
  rules:
  - apiGroups:
    - bitnami.com
    resources:
    - sealedsecrets
    verbs:
    - get
    - list
    - watch
    - create
    - patch
    - update
    - delete
